{
	"name": "recon-flow",
	"version": "0.0.1",
	"description": "Repeatable agentic recon: an AI composes the dataflow, a deterministic runtime executes it.",
	"license": "UNLICENSED",
	"private": true,
	"scripts": {
		"web": "forever src",
		"watch": "nodemon src",
		"demo": "node bin/demo.js",
		"demo-watch": "nodemon bin/demo.js",
		"lint": "eslint src/ bin/",
		"test": "jest",
		"test:watch": "jest --watch",
		"prepare": "husky"
	},
	"jest": {
		"testEnvironment": "node",
		"forceExit": true,
		"transformIgnorePatterns": [
			"/node_modules/(?!(uuid|sift)/)"
		]
	},
	"lint-staged": {
		"*.js": "eslint --fix"
	},
	"dependencies": {
		"got-verbose": "^0.0.18",
		"queue-promised": "^0.0.30",
		"sift": "^17.1.3",
		"uuid": "^14.0.1"
	},
	"devDependencies": {
		"@babel/core": "^8.0.1",
		"@babel/eslint-parser": "^8.0.1",
		"@babel/preset-env": "^8.0.2",
		"@eslint/js": "^10.0.1",
		"babel-jest": "^30.4.1",
		"eslint": "^10.8.0",
		"husky": "^9.1.7",
		"jest": "^30.4.2",
		"lint-staged": "^17.3.0",
		"nodemon": "^3.1.14"
	}
}
